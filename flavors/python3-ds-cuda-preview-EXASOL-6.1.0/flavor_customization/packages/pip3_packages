# This file specifies the package list which get installed via pip for python3.
# You can use the usual version notation of pip to specify the versions.
# We recommend to specify the the version with equals, such that the container 
# builds are reproducable and manage version updates as active step.
# As you can see, this file can contain comments which start with #.
# If a line starts with # the whole line is comment, however you can
# also start a comment after the package defintion.

# tensorflow-probability==0.9.0 # we specify here the exact version to install
